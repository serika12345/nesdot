import type { CSSProperties } from "react";

export const failureDetailLabelStyle: CSSProperties = {
  margin: 0,
  fontWeight: 500,
};

export const failureDetailValueStyle: CSSProperties = {
  margin: 0,
  overflowWrap: "anywhere",
  whiteSpace: "pre-wrap",
};
