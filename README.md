# NESDOT

NESDOT は、NES / ファミコンの画面制約を意識しながらドット絵を作るためのエディタです。現在は「スプライトを作る」「NES 画面上に配置して制約を確認する」というワークフローを中心に実装されています。

Tauri + React + TypeScript で構成されており、デスクトップアプリとして動かしつつ、フロントエンド単体でも確認できます。

## このプロジェクトでできること

### 1. スプライトを NES 風の制約で編集する

- 64 枚のスプライトスロットを持ちます
- 各スプライトは `8x8` または `8x16` で編集できます
- 描画ツールは `ペン` と `消しゴム`
- スプライトのクリアができます
- `8x8` 単位でブロックをドラッグして並べ替えできます
- パレットは 4 組あり、各パレットは 4 色スロットです
- `slot0` は透明扱いで、実際には描画されません

### 2. スプライトを画面に配置する

- 画面サイズは `256x240`
- 作成済みスプライトを X / Y 座標付きで配置できます
- 配置済みスプライトの一覧表示、位置変更、削除ができます
- 画面モードではスプライト総数とスキャンライン制約を検査します

### 3. NES 向けの出力を行う

- スプライト単位で `CHR` をエクスポートできます
- スプライト / 画面を `PNG` / `SVG` に出力できます
- プロジェクト全体を `JSON` で保存 / 復元できます

## 再現している制約

現時点でコード上に明示的に入っている制約は次のとおりです。

- スプライトの色は 4 色パレット単位で扱う
- パレットは 4 組
- 透明色は各パレットの `slot0`
- 画面上に配置できるスプライト総数は最大 `64`
- 同一スキャンライン上のスプライト数は最大 `8`
- スプライトサイズは `8x8` または `8x16`

## 現状の制限

README を書くにあたってコードを確認した範囲では、現状は次の状態です。

- 背景タイル (`32x30` の 8x8 背景) のデータ構造はありますが、UI から編集する機能はまだありません
- 画面モードは主に「スプライト配置プレビュー」と「制約チェック」に使う実装です
- 位置の画面外チェックは描画時にクリップされますが、追加時の厳密な拒否ロジックは無効化されています
- 自動永続化のための IndexedDB 実装はありますが、現在のストアでは無効化されており、実用上は JSON 保存 / 復元を使う形です
- テストコードはまだ入っていません

## 操作の流れ

### スプライトを作る

1. `編集モード` を `スプライト` にする
2. スプライト番号を選ぶ
3. `8x8` または `8x16` を選ぶ
4. パレットと色スロットを選んで描く
5. 必要なら `並べ替え` で 8x8 ブロックを入れ替える
6. `CHR` / `PNG` / `SVG` / `JSON` に出力する

### 画面を組む

1. `編集モード` を `画面` にする
2. 配置したいスプライト番号と座標を入力する
3. 画面に追加する
4. 一覧から配置済みスプライトを選んで位置調整または削除する
5. 画面全体を `PNG` / `SVG` / `JSON` に出力する

## セットアップ

### 前提ツール

- Node.js
- `pnpm`
- Rust / Cargo
- Tauri が要求する OS 依存ライブラリ

Linux では WebKitGTK などが必要です。このリポジトリには `flake.nix` があるため、Nix を使える環境なら開発依存をまとめて揃えられます。

### Nix を使う場合

```sh
nix develop
pnpm install
```

### Nix を使わない場合

必要な Node.js / pnpm / Rust / Tauri 依存を各自で入れたうえで、次を実行します。

```sh
pnpm install
```

## 開発コマンド

### フロントエンドのみ起動

```sh
pnpm dev
```

Vite 開発サーバーを起動します。

### Tauri デスクトップアプリを起動

```sh
pnpm start
```

`tauri dev` を起動します。`scripts/ensure-tauri-dev-server.mjs` により、`localhost:1420` のフロントエンド開発サーバーが存在すれば再利用し、なければ自動起動します。

### ビルド

```sh
pnpm build
```

TypeScript の型チェック付きでフロントエンドをビルドします。

### デスクトップアプリをパッケージ

```sh
pnpm package
```

または

```sh
pnpm make
```

どちらも `tauri build` を実行します。

### Lint

```sh
pnpm lint
```

## 技術構成

- UI: React 19
- 言語: TypeScript
- ビルド: Vite
- デスクトップ化: Tauri 2
- 状態管理: Zustand
- スタイル: Emotion
- ファイル保存 / 読み込み: Tauri Plugin Dialog / FS

## コード構成

- `src/App.tsx`
  - 画面モード / スプライトモードの切り替え
- `src/components/SpriteMode.tsx`
  - スプライト編集 UI と各種エクスポート
- `src/components/ScreenMode.tsx`
  - 画面へのスプライト配置と制約チェック
- `src/components/hooks/useSpriteCanvas.ts`
  - スプライト描画、ペン / 消しゴム、8x8 ブロック並べ替え
- `src/components/hooks/useScreenCanvas.ts`
  - 画面プレビュー描画
- `src/components/PalettePicker.tsx`
  - 4 組の NES パレット編集 UI
- `src/store/projectState.ts`
  - プロジェクト状態、画面合成、スプライト / パレットの型定義
- `src/nes/chr.ts`
  - 8x8 / 8x16 スプライトの CHR 変換
- `src/hooks/useExportImage.ts`
  - CHR / PNG / SVG / JSON の出力
- `src/hooks/useImportImage.ts`
  - JSON の復元
- `src-tauri/`
  - Tauri 側のエントリポイントと設定

## 備考

- ライセンスは `MIT` です
- ファイル入出力は Tauri ネイティブ API を優先し、使えない場合はブラウザのダウンロード / ファイル選択にフォールバックします

## 確認状況

この README 作成時点では、コード解析は完了していますが、この実行環境に `node` / `pnpm` / `cargo` が入っていなかったため、`pnpm build` や `pnpm lint` の実行確認までは行えていません。起動手順とコマンド説明は `package.json`、`flake.nix`、`src-tauri/tauri.conf.json` をもとに記述しています。
